<Window x:Class="ThLaunchSite.CreateAllGamesScoreBackupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ThLaunchSite"
        mc:Ignorable="d"
        Title="{Binding Resources.TitleCreateScoreBackup, Mode=OneWay, Source={x:Static local:ResourceService.Current}}" Height="254" Width="326" ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner" Loaded="WindowLoaded">
    <Grid>
        <ProgressBar
            x:Name="MainProgressBar" Height="24" Margin="10,10,10,0" VerticalAlignment="Top"
            Minimum="0"/>
        <TextBox x:Name="OutputBox"
                 Margin="10,39,10,10" TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 Text=""/>

    </Grid>
</Window>
